/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      appDir: true, // Pastikan ini ada kalau pakai App Router
    }
  }
  
  export default nextConfig
  